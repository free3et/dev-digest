import { SkillsView } from "./_components/SkillsView";

/* Route: /skills. Thin entry — grid, preview, editor and import live in _components/SkillsView. */
export default function SkillsPage() {
  return <SkillsView />;
}
